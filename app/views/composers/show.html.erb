<div class="page-body composers-page">
  <h1><%= "#{@composer.first_name} #{@composer.last_name}" %></h1>
  <div class="page-content composer-content">
    <%= render partial: "events/event_list", locals: {events: @composer.events.uniq, type: :full_date} %>
  </div>
</div>
