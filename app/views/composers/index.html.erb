<h1>Composer List</h1>

<table border="1">
  <% @composers.each do |composer| %>
    <tr>
      <td><%= composer.first_name %> </td>
      <td><%= link_to "#{composer.last_name}", composer %></td>
    </tr>
  <% end %>
</table>

