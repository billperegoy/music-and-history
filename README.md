# Getting Started
To setup the application to run in develpment mode, follow these steps":

1. git clone git@github.com:billperegoy/music-and-history.git
2. cd music-and-history
3. bundle install
4. rake db:migrate
5. rake db:seed
6. rails server

You shoud then be able to see the application by navigating to localhost:3000 in
a browser.

# Adminitrative Interface (CMS)
An administrative interface can be used to add or modify entries. This interface
can be accessed from localhost:3000/admin


